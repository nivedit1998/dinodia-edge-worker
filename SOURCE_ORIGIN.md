# Edge worker source origin

This directory was copied from the local source at:

`Tech Stack/dinodia-edge-worker`

The expected GitHub repository `nivedit1998/dinodia-edge-worker` was not available, and no Git metadata exists in the source directory. The source and Wrangler configuration were copied into `Tech Stack V2` without `node_modules` or `.wrangler` caches.

This is therefore a local source snapshot, not a Git clone. Add the correct remote repository when its GitHub location is confirmed.
